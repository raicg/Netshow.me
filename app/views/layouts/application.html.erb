<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Netshow.me</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://fonts.googleapis.com/css?family=Baloo+Bhai&display=swap" rel="stylesheet">
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>

  
  <!-- Navigation -->
  <nav class="navbar navbar-expand-xl navbar-light static-top border-bottom navbar-top">
    <div class="container">
      <%= link_to root_path do %>
        <h4 class="mb-0 text-dark">Netshow.me</h4>
      <% end %>
      <ul class="navbar-nav ml-auto">
        <%= render  "shared/menu" %>
      </ul>
    </div>
  </nav>

  <div class="container">
    <div>
      <%= render partial: "shared/flashes" %>
    </div>
    <% if yield(:right).presence %>
      <div class="float-right mt-4 mr-2">
        <%= yield :right %>
      </div>
      <br/>
    <% end %>
  </div>
  <!-- Page Content -->
  <div class="container" style="margin-top: 40px;">
    <div class="card border-0 my-5" style="min-height: 550px;">
      <div class="card-body p-5 text-center">
        <%= yield %>
      </div>
    </div>
  </div>

  <%= yield :scripts %>

</body>

</html>